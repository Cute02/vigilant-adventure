#include <stdio.h>
#include <stdlib.h>

int main(){
    int num;
    printf("Enter a number to find its square:");
    scanf("%d", &num);
    printf("The square of the entered number is: %d\n", num*num);
    return 0;
}